@using K9.Base.WebApplication.Helpers
@model EmailTemplate
@{
    Layout = "../Shared/_LayoutFormSmall.cshtml";
}
@using (Html.BeginForm(null, null, FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    using (Html.BeginBootstrapForm())
    {
        @Html.HiddenFor(model => model.Id)
        @Html.Partial("_EditFields")
        <hr />
        @Html.BootstrapButton(K9.Base.Globalisation.Dictionary.Update, EButtonType.Submit, "fa-save")
        @Html.BootstrapBackToListButton()
        
        @Html.BootstrapActionLinkButton(Dictionary.SendToMailingList, "send-to-mailinglist", "", new { id = Model.Id })
        @Html.BootstrapActionLinkButton(Dictionary.SendToToUser, "send-to-use", "", new { id = Model.Id })
        @Html.BootstrapActionLinkButton(Dictionary.Test, "test", "", new { id = Model.Id })
    }
}