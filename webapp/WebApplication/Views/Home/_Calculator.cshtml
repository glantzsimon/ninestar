@model K9.WebApplication.Models.NineStarModel

@using (Html.BeginForm("CalculateNineStarKi", "Home"))
{
    using (Html.BeginBootstrapForm())
    {
        @Html.BootstrapEditorFor(model => model.DateOfBirth)
        @Html.BootstrapEditorFor(model => model.Gender)

        <hr/>
        @Html.BootstrapButton(K9.Globalisation.Dictionary.Calculate)
    }
}