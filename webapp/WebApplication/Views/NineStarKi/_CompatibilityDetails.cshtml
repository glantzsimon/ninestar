@model K9.WebApplication.Models.CompatibilityModel

<div class="row">
    <div class="col-sm-12">
        @if (Model != null && Model.IsProcessed)
        {
            <h4 class="margin-top-0">@Dictionary.ChemistryScore</h4>
            
        }
    </div>
</div>