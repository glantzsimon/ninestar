@using K9.WebApplication.Helpers
@using K9.WebApplication.Models
@model NineStarKiModel

@if (Model != null)
{
    <div class="container main-content clear-both">
        <article>
            <div>
                @Html.Panel(Model.EnergySexualityLabel, Model.MainEnergySexualityDetails)

                @Html.Panel(Model.SexualityRelationTypeLabel, @Html.Partial("_SexualityRelationType").ToString())
            </div>
        </article>
    </div>



}