@using K9.WebApplication.Helpers
@using K9.WebApplication.Models
@using K9.WebApplication.Options
@model NineStarKiModel

@if (Model != null)
{
    <div class="container main-content clear-both">
        @Html.CollapsiblePanel(Dictionary.AdultChildRelationshipTitle, Dictionary.AdultChildRelationship)

        @Html.Panel(Dictionary.AdultChildRelationshipTitle, Model.CharacterEnergy.ChildDescription, "", "", EPanelImageSize.Default, EPanelImageLayout.Contain)
    </div>
}