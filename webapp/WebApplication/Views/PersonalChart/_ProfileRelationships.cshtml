@using K9.WebApplication.Helpers
@using K9.WebApplication.Models
@model NineStarKiModel

@if (Model != null)
{
    <div class="container main-content clear-both">
        <div>
            @Html.PanelWithSummary(Model.RelationshipsLabel, Model.MainEnergyRelationshipsDetails, Model.MainEnergyRelationshipsSummary)

            @Html.Panel(Dictionary.SexualityGenderRelationLabel, Html.Partial("_ProfileSexualityRelationType").ToString())
        </div>
    </div>
}