@using K9.WebApplication.Helpers
@using K9.WebApplication.Models
@using K9.WebApplication.Options
@model NineStarKiModel

@if (Model != null)
{
    <div class="container main-content clear-both">
        @Html.Panel(Dictionary.StressResponse, $"{Model.StressResponseDetails} <br/> {Model.StressResponseFromNatalHouseDetails}", "", "", EPanelImageSize.Default, EPanelImageLayout.Contain)
    </div>
}