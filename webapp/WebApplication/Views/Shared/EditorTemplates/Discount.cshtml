@using K9.Base.WebApplication.Extensions
@using K9.Base.WebApplication.Helpers
@using K9.DataAccessLayer.Enums
@using K9.WebApplication.Enums
@model K9.DataAccessLayer.Enums.EDiscount
@{
    var selectList = this.GetDropdownData().GetSelectListFromEnum<EDiscount>(Convert.ToInt32(Model), true); 
}
<div class="desktop-only">
    @Html.BootstrapDropdownFor(model => model, selectList)
</div>
<div class="device-only">
    @Html.DropDownListFor(model => model, selectList, new { @class = "form-control" })
</div>