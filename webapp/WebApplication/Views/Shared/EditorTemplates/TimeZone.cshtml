@using K9.Base.WebApplication.Extensions
@using K9.SharedLibrary.Models
@using K9.WebApplication.Enums
@using K9.WebApplication.Extensions
@model string
@{
    var selectList = this.GetDropdownData().GetSelectListFromCustomDataset<K9.DataAccessLayer.Models.TimeZone>(K9.DataAccessLayer.Models.TimeZone.ExtendedTimeZones, Model);
}

@if (this.GetDeviceType() == EDeviceType.Desktop)
{
    @Html.BootstrapDropdownFor(model => model, selectList)
}
else
{
    @Html.DropDownListFor(model => model, selectList, new { @class = "form-control" })
}
